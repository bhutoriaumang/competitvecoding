#include </Users/bits/stdc++.h>
using namespace std;

int main(){
    int n,e;
    cin>>n>>e;
    vector < vector <int> > v(e,vector <int>);
    for(int i=0;i<e;i++){
        int x,y,s;
        cin>>x>>y>>d;
        v[i].push_back[d];
        v[i].push_back[x];
        v[i].push_back[y];
    }
    sort(v.begin(),v.end());
    vector < vector <int> > ans;
    while(ans.size()<(n-1)){
        
    }
}