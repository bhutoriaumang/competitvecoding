#include <bits/stdc++.h>
using namespace std;

int main(){
    int T;
    cin>>T;
    while(T-->0){
        string s;
        cin>>s;
        vector <int> v(26,0);
        sort(s.begin(),s.end());
        cout<<s<<endl;
    }
}